# Agent 流式稳定性基线（2026-08-03）

## 结论

本次真实运行覆盖现有 12 条 Agent golden cases：12/12 收到合法且完整的流，12/12 成功结束，未出现 `mixed_model_output`。这是一轮本地基线，只能证明本次配置下链路跑通，不能据此声称长期混合输出率为 0。

## 运行信息

- 时间：2026-08-03 CST
- 模型：`kimi-k2.6`
- 数据集：`agent/experiments/datasets/agent_eval_cases.json`（12 cases）
- 执行入口：`agent_app.scripts.evaluate_agent_stream`
- 被测边界：直接消费 `stream_agent_events()`；不包含 FastAPI、Next.js BFF 和浏览器网络开销
- 环境：本地 Qdrant `documents` collection、Ollama embedding、仓库 uv workspace

运行命令：

```bash
uv run python -m agent_app.scripts.evaluate_agent_stream
```

## 汇总指标

| 指标 | 结果 |
| --- | ---: |
| 完整流 | 12/12（100%） |
| 协议有效 | 12/12（100%） |
| 流成功 | 12/12（100%） |
| 正常结束 | 12/12（100%） |
| `mixed_model_output` | 0/12（0%） |
| 平均首事件延迟 | 7.796s |
| P95 首事件延迟 | 23.754s |
| 平均首答案延迟 | 11.683s |
| P95 首答案延迟 | 43.904s |
| 平均总耗时 | 25.082s |
| P95 总耗时 | 77.452s |

P95 使用现有评测代码的 nearest-rank 算法；12 个样本下 P95 会取本次最大观测值。平均延迟包含两个接近 0 秒的空问题单步降级 case，因此不能直接代表知识检索问题的平均体验。

## 代表性 case

| Case | 步骤 | 来源 | 首事件 | 首答案 | 总耗时 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `direct_greeting` | 0 | 0 | 2.804s | 2.804s | 2.806s |
| `rag_definition` | 1 | 7 | 8.386s | 9.237s | 25.723s |
| `langchain_vs_llamaindex` | 1 | 7 | 14.206s | 14.949s | 40.358s |
| `chroma_vs_qdrant` | 3 | 28 | 23.754s | 43.904s | 66.389s |
| `agent_framework_multi_source` | 2 | 14 | 13.674s | 34.055s | 77.452s |

## 数据安全检查

生成的 JSON 只保留问题、事件类型、工具名、计数、终止元数据和时间指标。人工扫描字段路径后确认没有 `answer`、`sources`、`snippet`、`tool_args` 或 `message` 字段。原始运行文件位于被 Git 忽略的 `agent/experiments/runs/streaming/`，不提交仓库。

## 解读与下一步

本轮没有复现浏览器 smoke 中曾出现的混合输出，说明单次失败不足以证明它是高频问题。下一步不应立即放宽协议；应优先对最容易触发多步调用的 3 个 case 做多次重复运行，给出带分母的混合输出率和延迟分布，再决定是否收紧 Agent Prompt。
